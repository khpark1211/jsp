Ajax(Asynchronous JavaScript and XML) : 비동기 통신
 
 - 서버 와 클라이언트간의 속도를 맞출 필요가 없다 (비동기)
   (클라이언트가 서버에게 서비스를 요청하고 응답이 올때까지 클라이언트는 기다린다.(동기)
       하지만 비동기란 클라이언트가 서비스를 요청하고 서버의 응답을 기다리지 않고 다른 작업을 
       할 수 있다.)
 - 페이지단위의 로드가 아니라, 파일 조각 단위로 로드 한다.
      즉 , 전체 페이지를 다시 로드하지 않고  페이지의 일부만 동적으로 갱신할 수 있다.